<html>
    <head>
        <title>
            Home
        </title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <link rel="stylesheet" href="/css/home.css">
        <script src="https://kit.fontawesome.com/d76b9d75bf.js" crossorigin="anonymous"></script>
    </head>

    <body>
<div id="intro">

<h1 id="heading">Authentication Application </h1>
<p>This application uses passport strategies(local,google-oauth), passport session to maintain user session,<br> DB used is mongoDB, noty is used for pushing notifications. Nodemailer is implemented for sending mails for forget password functionality. </p>
<p>Developed by Anirudh Bhat</p><br>
<img src="/images/lock.jpg" width="410" height="300">
</div>
        <section id="left">
            <div id="left-grid">
                <h1>Sign in</h1>
                <form action="/sign-in" method="get">
                    <input type="email" placeholder="ironman@avengers.com" name="email" required><br>
                    <input type="password" placeholder="tonystark" minlength="6" maxlength="10" name="password" required><br>
                    <input type="submit" value="Submit" id="sign-in">
                </form>
                <div id="i-container">
                    <i class="fa-brands fa-google">
                        <a href="/sign-up/auth/google"></a>
                    </i>
                <a href="/forgot-password" id="forgot-password">Forgot?</a>
            </div>
        </section>

        <section id="right">
            <div id="right-grid">
                <h1></h1>
                <h1 id="sign-up">Sign Up</h1>
                <h5>With</h5>
                <div id="i-container">
                    <i class="fa-solid fa-envelope">
                        <a href="/sign-up/email"></a>
                    </i>
                </div>
                
                </div>
            </div>
        </section>

        <script>
            <% if(flash.success && flash.success.length > 0){ %>
                new Noty({
                    theme: 'relax',
                    text: '<%= flash.success%>',
                    type: 'success',
                    layout:'topRight',
                    timeout: 1000
                }).show()
            <% } %>
                    
            <% if(flash.error && flash.error.length > 0){ %>
                new Noty({
                    theme: 'relax',
                    text: '<%= flash.error%>',
                    type: 'error',
                    layout:'topRight',
                    timeout: 1000
                }).show()
            <% } %>
        </script>
    </body>
</html>